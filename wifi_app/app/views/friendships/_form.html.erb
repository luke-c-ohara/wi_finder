<%= form_for(@friendship) do |f| %>
  <% if @friendship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@friendship.errors.count, "error") %> prohibited this friendship from being saved:</h2>

      <ul>
      <% @friendship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>

  </div>
  <div class="field">
    <%= f.label :friend_id %><br />
    <%= f.collection_select :friend_id, User.all, :id, :email %>
  </div>
  <div class="field">
    <%= f.label :network_id %><br />
    <%= f.collection_select :network_id, current_user.networks, :id, :nickname %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>




