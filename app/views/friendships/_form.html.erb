<%= form_for @network do |f| %>
  <% if @network.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@network.errors.count, "error") %> prohibited this friendship from being saved:</h2>

      <ul>
      <% @network.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
   <%= f.hidden_field :user_id, :value => current_user.id %>
 </div>

  <div class="field">

  </div>

  <div class="field">
    <%= f.label :friend_id %><br />
    <%= f.collection_select :friend_id, @members, :id, :email %>
  </div>
  <div class="field">
    <%= f.label :network_id %><br />
    <%= f.hidden_field :network_id, :value => @network.id %>
  </div>
  <div class="actions">
    <%= f.submit value="Share network" %>
  </div>
<% end %>




